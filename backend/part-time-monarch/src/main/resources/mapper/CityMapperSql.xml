<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tidc.part.time.monarch.mapper.CityMapper">

    <select id="selectCityAll" resultType="com.tidc.part.time.monarch.domain.City">
        SELECT * FROM city WHERE id > 0
    </select>

    <select id="findCityByName" parameterType="String" resultType="com.tidc.part.time.monarch.domain.City">
        select * from city where name = #{name}
    </select>

    <select id="findCityIdByName" parameterType="String" resultType="int">
        select id from city where name = #{name}
    </select>
</mapper>